<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cos.blog.repository.UserRepository">
 
 <insert id="save">
          INSERT INTO post(title, content, userId, createDate) VALUE(#{title}, #{content}, #{userId}, now())
 </insert>
 
 <select id="findAll" resultType="com.cos.blog.model.Post">
         SELECT id, title, content, userId, createDate, FROM post ORDER BY id DESC 
 </select>
  
</mapper>